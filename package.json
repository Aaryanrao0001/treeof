{
  "name": "infinite-messaging-tree",
  "version": "1.0.0",
  "description": "A living, magical, ever-expanding digital organism where every message becomes a glowing node on a magical tree.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
